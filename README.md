# cypressTest
cypress e2e test for to do web app

How to Start

1. Clone the project
2. npm i - Install all the dependencies
3. Run "npx cypress open" in your directory to execute the tests in Test Runner | Run "npx cypress run" to execute the tests in CLI
